# Demo app
This is an example app, it uses:
  - NestJS
  - CQRS pattern
  - TypeORM
  - Docker
  - PostgreSQL

# To run the application:
  - Go to project root
  `docker-compose up -d`
  - Go to the backend folder
  `yarn run start`
